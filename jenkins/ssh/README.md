# SSH keys

Este directorio no debe contener claves privadas ni públicas reales.

Las credenciales SSH deben generarse fuera del repositorio y almacenarse
mediante Jenkins Credentials o secretos montados en tiempo de ejecución.